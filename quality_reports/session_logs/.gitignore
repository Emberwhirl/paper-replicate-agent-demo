# Session logs are local-only working notes (one per session).
# Keep this directory under version control, but never track the logs here —
# they stay on your machine only and must not be committed to this branch.
*.md
